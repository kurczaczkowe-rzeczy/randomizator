import selectMockData from 'components/Select/Select.mock';
import fileContainerMockData from 'components/FileContainer/FileContainer.mock';

const mockData = {
  answersCounter: 15,
  link: 'link/to/form',
  selectFormsProps: selectMockData,
  fileContainerProps: fileContainerMockData,
};

export default mockData;
