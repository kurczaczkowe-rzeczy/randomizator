import { ReactNode } from 'react';

/** An object describe content controller for answer. */
export interface IAnswerContentController {
  /** This value specify value send by form from user. */
  answer: ReactNode;
}
